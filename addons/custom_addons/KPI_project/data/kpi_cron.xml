<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="ir_cron_project_kpi_snapshot" model="ir.cron">
        <field name="name">Project KPI: Take manager workload snapshot</field>
        <field name="model_id" ref="model_project_kpi_snapshot"/>
        <field name="state">code</field>
        <field name="code">model._take_snapshot()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">weeks</field>
        <field name="active">True</field>
    </record>

    <record id="ir_cron_project_kpi_tasks_completed_snapshot" model="ir.cron">
        <field name="name">Project KPI: Take tasks completed per week snapshot</field>
        <field name="model_id" ref="model_project_kpi_tasks_completed_snapshot"/>
        <field name="state">code</field>
        <field name="code">model._take_snapshot()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">weeks</field>
        <field name="active">True</field>
    </record>

    <record id="ir_cron_project_kpi_planning_snapshot" model="ir.cron">
        <field name="name">Project KPI: Take task planning completeness snapshot</field>
        <field name="model_id" ref="model_project_kpi_planning_snapshot"/>
        <field name="state">code</field>
        <field name="code">model._take_snapshot()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>

    <record id="ir_cron_project_kpi_staleness_snapshot" model="ir.cron">
        <field name="name">Project KPI: Take project staleness snapshot</field>
        <field name="model_id" ref="model_project_kpi_staleness_snapshot"/>
        <field name="state">code</field>
        <field name="code">model._take_snapshot()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>
</odoo>
